---
title: "Analytics"
description: "Usage summaries, agent performance, feedback analysis, adoption metrics, and capacity planning"
---

AI Insights provides multiple analytics endpoints covering different aspects of platform usage.

## Summary

```
GET v1/analytics/summary
```

High-level platform analytics:
- Total conversations, messages, and active users
- Average response time and satisfaction scores
- LLM token usage and cost
- Trend data (vs. previous period)

## Agent Analytics

```
GET v1/analytics/agents
```

Per-agent performance metrics:

| Metric | Description |
|--------|-------------|
| Conversation count | Total conversations per agent |
| Average evaluation score | LLM judge score |
| Resolution rate | Percentage of resolved conversations |
| Sentiment distribution | Breakdown of positive/neutral/negative |
| Token usage | Input/output tokens per conversation |

Supports filtering by:
- `agent_ids` — Array of agent IDs
- `from_date` / `to_date` — Date range

## Feedback Analytics

```
GET v1/analytics/feedback
```

Aggregated user feedback (like/dislike votes):
- Vote distribution (like vs. dislike)
- Feedback trends over time
- Category breakdown (tallied from dislike categories)
- Per-agent feedback breakdown

### Submit Feedback

```
POST v1/feedback
```

```json
{
  "vote": "like",
  "task_id": "task_abc",
  "message_id": "msg_xyz",
  "context_id": "conv_abc",
  "agent_id": "agent_abc"
}
```

For dislike votes, additional fields are available:
- `category` — One of: `incorrect_incomplete`, `not_what_i_asked`, `slow_buggy`, `style_tone`, `safety_legal`, `other`
- `comment` — Optional free-text feedback

Feedback is listed via `GET v1/analytics/feedback`.

## Queue Statistics

```
GET v1/analytics/queue-stats
```

Analysis queue health:
- Pending analyses
- Processing rate
- Average processing time
- Queue depth over time
- Failed analyses and retry count

## Capacity Analytics

```
GET v1/analytics/capacity
```

Analysis queue capacity metrics:
- Queue depth and processing count
- Processing rate per hour
- Failed analyses in last hour
- Estimated completion time

## Adoption Analytics

```
GET v1/analytics/adoption
```

Memory-based adoption metrics:
- Adoption tiers (power/regular/basic users)
- Personalization statistics
- Common memory patterns

## Organization-Level Aggregation

Organization analytics are aggregated hourly by a scheduled job (`10 * * * *`) and stored in the `org_analytics` collection.

```
POST v1/admin/refresh-org-analytics
```

Force-refresh organization analytics (admin only). Useful after large data imports or migrations.

## Insights Status

```
GET v1/insights/status
```

Returns the analysis status for the current organization:
- Total conversations analyzed
- Pending analyses
- Last analysis timestamp
- Analysis coverage percentage
