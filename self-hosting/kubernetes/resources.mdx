---
title: "Resource Allocation"
description: "CPU and memory recommendations for Kubernetes deployments"
---

## Service Resources

| Service | CPU Request | CPU Limit | Memory Request | Memory Limit |
|---------|------------|-----------|----------------|--------------|
| API Gateway | 500m | 1000m | 512Mi | 1Gi |
| Runtime | 1000m | 2000m | 1Gi | 2Gi |
| Workspaces | 250m | 500m | 256Mi | 512Mi |
| Events | 250m | 500m | 256Mi | 512Mi |
| Platform Frontend | 50m | 200m | 64Mi | 128Mi |
| Pages | 250m | 500m | 256Mi | 512Mi |

## Database Resources

| Component | CPU | Memory | Storage |
|-----------|-----|--------|---------|
| MongoDB/PostgreSQL (per node) | 2 vCPU | 2GB | 10GB |
| Elasticsearch (per node) | 4 vCPU | 8GB | 400GB (NVMe/SSD) |
| Redis (per node) | 2 vCPU | 3GB | — |

## Resource Quotas

Apply namespace-level quotas to prevent resource exhaustion:

```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: prisme-quota
  namespace: prisme
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "16Gi"
    limits.cpu: "16"
    limits.memory: "32Gi"
```

## Limit Ranges

Set defaults for pods without explicit resource specs:

```yaml
apiVersion: v1
kind: LimitRange
metadata:
  name: prisme-limits
  namespace: prisme
spec:
  limits:
    - default:
        cpu: "500m"
        memory: "512Mi"
      defaultRequest:
        cpu: "100m"
        memory: "128Mi"
      type: Container
```

## Monitoring

Set up Prometheus alerts for resource pressure:

```yaml
groups:
  - name: prisme-resources
    rules:
      - alert: HighMemoryUsage
        expr: container_memory_usage_bytes{namespace="prisme"} / container_spec_memory_limit_bytes > 0.85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High memory usage in {{ $labels.pod }}"
```
