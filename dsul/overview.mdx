---
title: "Overview"
description: "How Prisme.ai workspaces are built: a standard React front-end and versionable YAML automations, open and reversible."
---

A Prisme.ai workspace is built the way your developers already work, not locked into a proprietary language. The **front-end is a standard React application**. The **backend logic** (automations and agentic workflows) is described in **YAML**, for one practical reason: it versions cleanly in Git and stays reproducible. Together they form the workspace's declarative, versionable definition, what we call **DSUL**: the [Infrastructure-as-Code](/get-started/platform-overview#the-development-lifecycle) layer that keeps agents and applications reproducible instead of scattered across silos.

## Open, not locked in

Declarative platforms often raise the same fear: getting trapped in a proprietary language you cannot leave. Prisme.ai is built the opposite way.

<CardGroup cols={2}>
  <Card title="A framework your devs know" icon="react">
    The front-end is a normal **React** single-page application. No proprietary UI language to learn. You create it in **VS Code** from the [starter SPA](https://github.com/prismeai/starter-spa).
  </Card>
  <Card title="YAML only where it earns its keep" icon="file-code">
    Automations are YAML for one reason: they **version and review in Git**. It is a practical choice for reproducibility, not a cage.
  </Card>
  <Card title="Open and reversible" icon="unlock">
    Open-source, exportable, and portable. Your code lives in **your** Git repository, so you can always take it with you.
  </Card>
  <Card title="Scoped code access by design" icon="shield-halved">
    Writing raw code is deliberately limited to reduce the risk of **security issues and data leaks**. Most builders stay in safe, governed building blocks; code is opened only where it is needed.
  </Card>
</CardGroup>

## Everything is a workspace

The unit DSUL describes is a **workspace**: a self-contained, full-stack application that runs on the platform. Every product (Chat, Agent Creator, Knowledges, and the others) is itself a workspace. [Builder](/products/ai-builder/overview) is the tool that creates workspaces and opens them to edit.

<Frame>
  <img src="/images/concept-workspace.svg" alt="A workspace bundles Pages (React UI), Automations (agentic workflows), Imports (apps and SDKs, pinned), Files, and Activity. Products are workspaces too, opened and edited in Builder." />
</Frame>

## Apps are SDKs

Publish a workspace and it becomes a versioned **App** in the [App Store](/apps-store/overview), behaving like an SDK. Import that App into another workspace and its instructions become available inside your automations. It is workspaces composing workspaces.

<Frame>
  <img src="/images/concept-sdk.svg" alt="Publish a workspace as a versioned App in the App Store, then import it into another workspace so its instructions enrich that workspace's automations. Agent Creator is the product; Agent Factory App is the importable SDK." />
</Frame>

## How you build a workspace

<Steps>
  <Step title="Start from the starter SPA">
    Clone the [starter SPA](https://github.com/prismeai/starter-spa) in **VS Code**. It gives you a standard React project wired to the platform.
  </Step>
  <Step title="Build the front and the automations">
    Develop the **React** front-end as usual, and describe the backend logic as **YAML** automations alongside it.
  </Step>
  <Step title="Version with Git">
    Commit to your own repository. Every change is reviewable through a merge request. See [versioning](/products/ai-builder/versioning).
  </Step>
  <Step title="Push to a workspace">
    Deploy to a sandbox for fast iteration, or promote through Git for a structured, auditable release. See [deployment](/products/ai-builder/deployment).
  </Step>
</Steps>

## Core components

- **Pages**: the front-end, a standard **React** SPA (built in VS Code from the [starter SPA](https://github.com/prismeai/starter-spa))
- **Automations**: backend logic and agentic workflows, described in **YAML** so they version in Git
- **Imports**: connectors, apps, MCP servers, and custom code installed into the workspace
- **Files**: documents and assets used by the workspace
- **Activity**: the workspace's execution events, for observability
- **RBAC**: fine-grained access control over all of the above

## Why this model for the enterprise

- **A framework devs already know**: React for the front-end, YAML only for automations, no proprietary stack to adopt.
- **Reproducible by default**: everything lives in Git, with merge requests and environment promotion. See [versioning](/products/ai-builder/versioning) and [deployment](/products/ai-builder/deployment).
- **Shared, standardized infrastructure**: many workspaces run on the same multi-tenant platform with logical isolation, instead of dedicated infrastructure per agent. See [Platform Overview](/get-started/platform-overview).
- **Cloud and LLM agnostic**: the same workspace runs on any major cloud or on-premises, with no vendor lock-in.
- **Native observability and traceability**: every action is a normalized event with a correlation ID, so monitoring and audit are built in.
- **Governed by design**: RBAC, audit trails, and central policy across every workspace, with code access scoped to limit security and leak risk. See [Governe](/products/ai-governance/overview).

## Next steps

<CardGroup cols={2}>
  <Card title="Starter SPA" icon="github" href="https://github.com/prismeai/starter-spa">
    The React starter to create a workspace in VS Code
  </Card>
  <Card title="Builder" icon="screwdriver-wrench" href="/products/ai-builder/overview">
    Create and edit workspaces: pages, automations, imports, and activity
  </Card>
  <Card title="App Store" icon="store" href="/apps-store/overview">
    Publish and import Apps and MCP servers as reusable capabilities
  </Card>
  <Card title="Platform Overview" icon="sitemap" href="/get-started/platform-overview">
    Architecture, development lifecycle, and governance
  </Card>
</CardGroup>
